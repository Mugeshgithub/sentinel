# Sentinel eval harness — fintech example cases
# Replace repo/rules paths with your own before running.
repo: "/path/to/your/repo"
rules: "/path/to/your/repo/.sentinel/rules.yaml"

cases:
  - id: penny-stocks-in-movers
    fix_commit: abc1234
    note: 'fix(overview): sort movers by marketCap'
    expected_rules:
      - unfiltered-market-data
      - manual-symbol-conversion

  - id: research-tile-global-feed
    fix_commit: def5678
    note: 'fix(research): tile + drawer use same filtered API'
    expected_rules:
      - incomplete-api-params
      - tile-drawer-data-source

  - id: legacy-provider-dead-refs
    fix_commit: ghi9012
    note: 'refactor(data): remove legacy data provider + dead routes'
    expected_rules:
      - dead-endpoint-references
